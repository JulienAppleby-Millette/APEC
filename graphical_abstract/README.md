# Paper 1 Graphical Abstract

Generated by:

```bash
python scripts/gen_graphical_abstract.py
```

Outputs:

- `paper1_graphical_abstract.svg`: editable vector master.
- `paper1_graphical_abstract.pdf`: vector export for manuscript systems.
- `paper1_graphical_abstract.png`: high-resolution raster preview/export.
- `paper1_graphical_abstract_no_orbitals.*`: explicit no-orbital option; currently identical to the canonical export.
- `paper1_graphical_abstract_orbitals.*`: optional variant with subtle Ti-centered orbital lobes in the DFT/DFPT panel.

Intended message:

Posterior error certificates attach calibrated uncertainty, provenance, observable typing, and validity scope to DFT-derived material properties. In BaTiO3, the soft-mode dependence of electro-optic response amplifies uncertainty in the phonon frequency into larger uncertainty in `r33`; certificate decomposition and phase-aware fusion make this propagation visible and reusable.

Design notes:

- The figure is schematic rather than a replacement for manuscript Fig. 2.
- The `r33 = 108 +/- 25 pm/V` value and `~24%` fused uncertainty follow the June 29 final manuscript/SI framing. The single-calculation derived certificate is `28.4%`, not the older stale `27.0%` value.
- The JSON snippet is intentionally abbreviated for readability.
- The canonical export is identical to the no-orbital variant, which is visually cleaner at small size; the orbital variant is available if a stronger atomistic cue is preferred.
